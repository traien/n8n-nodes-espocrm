// Entry point for n8n community nodes package
// Exporting is optional for n8n, but providing a valid main prevents load errors when requiring the package.
// n8n loads nodes/credentials based on the "n8n" field in package.json pointing to compiled dist paths.

module.exports = {};
